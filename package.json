{
  "name": "seld-extension",
  "version": "0.3.1",
  "type": "module",
  "description": "Sinhala-English Learners Dictionary Lookup Extension",
  "scripts": {
    "predev": "node scripts/generate-build-info.js && node scripts/inject-changelog.js && node scripts/check-freshness.js && node scripts/extract-lists.js && node scripts/build-bloom.js && node scripts/run-tests-prompt.js",
    "dev": "wxt",
    "prebuild": "node scripts/generate-build-info.js && node scripts/inject-changelog.js && node scripts/check-freshness.js && node scripts/extract-lists.js && node scripts/build-bloom.js && node scripts/run-tests-prompt.js",
    "build": "wxt build",
    "prezip": "node scripts/generate-build-info.js && node scripts/inject-changelog.js && node scripts/check-freshness.js 24 && node scripts/extract-lists.js && node scripts/build-bloom.js",
    "zip": "wxt zip",
    "compile": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tippy.js": "^6.3.7"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitest/ui": "^4.1.0",
    "@wxt-dev/module-react": "^1.1.2",
    "fast-xml-parser": "^5.5.6",
    "npm-run-all": "^4.1.5",
    "sharp": "^0.34.5",
    "typescript": "^5.5.0",
    "vitest": "^4.1.0",
    "wxt": "^0.19.10"
  }
}